import React from 'react'
import { User } from '@/models/user.model';

interface Props {
  user: User
}

const BusinessTemplate = ({user}: Props) => {
  return (
    <div>BusinessTemplate</div>
  )
}

export default BusinessTemplate;
